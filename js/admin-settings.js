import { adminShell } from './admin-auth.js';
import { $,icon,esc,adminNumber,validPhone,normalizePhone,confirmDialog,toast,errorState } from './common.js';
import { setSetting,clearAllData,getAllAssessments } from './db.js';
import { seedDemo } from './seed.js';
async function init(){if(!adminShell('settings','Pengaturan'))return;const main=$('#main-content');try{main.innerHTML=`<div class="admin-title"><div><span class="eyebrow">PREFERENSI WORKSPACE</span><h1>Pengaturan</h1><p>Kelola tujuan WhatsApp dan data pada perangkat demo ini.</p></div></div><div class="settings-grid"><section class="card"><span class="tag">Mode: Local Prototype</span><h2 style="margin-top:20px">Penyimpanan di browser ini</h2><p>Assessment tersimpan di IndexedDB pada browser dan origin ini (${esc(location.origin)}). Data dari perangkat atau browser lain tidak otomatis muncul di sini. Login admin adalah gerbang demo, bukan autentikasi production.</p><div class="callout info">${icon('shield')} Gunakan data fiktif saat demonstrasi. Tidak ada sinkronisasi cloud atau klaim enkripsi data.</div><p id="data-count"></p></section><section class="card"><h2>WhatsApp konsultasi</h2><p>Nomor tujuan ringkasan assessment dan tombol konsultasi publik.</p><form id="settings-form"><label class="form-field">Nomor WhatsApp admin<input name="whatsapp" type="tel" required value="${esc(await adminNumber())}" placeholder="6282319199534" aria-describedby="settings-error"></label><p class="form-error" id="settings-error" role="alert"></p><div class="actions"><button class="btn" type="submit">Simpan Pengaturan</button></div></form></section><section class="card"><h2>Data demonstrasi</h2><p>Reset mengganti hanya record berlabel demo dengan 8 pasien fiktif. Assessment yang Anda isi sendiri tetap tersimpan.</p><button class="btn btn-light" id="reset-demo">Reset Demo Data</button></section><section class="card"><h2>Hapus semua data lokal</h2><p>Menghapus seluruh assessment, catatan, aktivitas, dan pengaturan WhatsApp pada browser ini. Aksi ini tidak dapat dibatalkan. Data demo tidak akan muncul kembali kecuali di-reset secara manual.</p><button class="btn btn-danger" id="clear-all">Clear All Data</button></section></div>`;
 async function count(){const all=await getAllAssessments();$('#data-count').textContent=`${all.length} assessment tersimpan · ${all.filter(a=>a.isDemo).length} record demo`;}await count();
 $('#settings-form').addEventListener('submit',async e=>{e.preventDefault();const n=e.target.elements.whatsapp.value;$('#settings-error').textContent='';if(!validPhone(n)){$('#settings-error').textContent='Masukkan nomor valid diawali 08 atau +62.';e.target.elements.whatsapp.focus();return;}const btn=e.target.querySelector('button');btn.disabled=true;try{await setSetting('whatsappNumber',normalizePhone(n));toast('Pengaturan WhatsApp tersimpan.');}catch{toast('Pengaturan belum tersimpan.',true);}finally{btn.disabled=false;}});
 $('#reset-demo').addEventListener('click',async e=>{if(await confirmDialog('Reset data demo?','Catatan dan perubahan pada record demo akan diganti. Assessment yang Anda buat tetap tersimpan.')){e.target.disabled=true;try{await seedDemo(true);await count();toast('8 assessment demo berhasil disiapkan.');}catch{toast('Reset belum berhasil. Coba kembali.',true);}finally{e.target.disabled=false;}}});
 $('#clear-all').addEventListener('click',async e=>{if(await confirmDialog('Hapus seluruh data lokal?','Semua assessment, catatan, aktivitas, dan pengaturan akan dihapus permanen dari browser ini.',{typed:true})){e.target.disabled=true;try{await clearAllData();sessionStorage.removeItem('sa3AssessmentDraft');$('#settings-form').elements.whatsapp.value=await adminNumber();await count();toast('Seluruh data lokal berhasil dihapus.');}catch{toast('Data belum berhasil dihapus.',true);}finally{e.target.disabled=false;}}});
 }catch{errorState(main);}}
init();
